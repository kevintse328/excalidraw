version: 2.1

workflows:
  cache-benchmark:
    jobs:
      - benchmark-no-cache
      - benchmark-warm-cache
      - benchmark-cold-miss

jobs:
  benchmark-no-cache:
    docker:
      - image: cimg/node:22.0
    resource_class: medium
    steps:
      - checkout
      - run:
          name: Install dependencies (no cache)
          command: npm ci --prefer-offline=false --ignore-scripts

  benchmark-warm-cache:
    docker:
      - image: cimg/node:22.0
    resource_class: medium
    steps:
      - checkout
      - restore_cache:
          keys:
            - excalidraw-deps-v1-{{ checksum "package-lock.json" }}
      - run:
          name: Install dependencies
          command: npm ci --ignore-scripts
      - save_cache:
          key: excalidraw-deps-v1-{{ checksum "package-lock.json" }}
          paths:
            - ~/.npm

  benchmark-cold-miss:
    docker:
      - image: cimg/node:22.0
    resource_class: medium
    steps:
      - checkout
      - restore_cache:
          keys:
            # Intentionally wrong key — will always miss, measures failed restore overhead
            - excalidraw-deps-v1-cache-miss-{{ epoch }}
      - run:
          name: Install dependencies (after cache miss)
          command: npm ci --prefer-offline=false --ignore-scripts
